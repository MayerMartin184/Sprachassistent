[project]
name = "sprachassistent"
version = "0.1.0"
description = "Sprachgesteuerter Desktop-Assistent: Aufgaben, Recherche, Listen, E-Mail und Dateiablage (Claude + Microsoft 365)"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=1.3.0",
    "msal>=1.28",
    "requests>=2.31",
    "pydantic-settings>=2.2",
    "sounddevice>=0.4.6",
    "numpy>=1.26",
    "openwakeword>=0.6",
    "onnxruntime>=1.16",
    "tzdata>=2024.1",
    "pywebview>=5.0",
    "pillow>=10.0",
    "send2trash>=1.8",
    "pypdf>=4.0",
    "python-docx>=1.1",
    "openpyxl>=3.1",
    "python-pptx>=1.0",
]

[project.optional-dependencies]
dev = ["pytest>=8"]
webcam = ["opencv-python-headless>=4.9"]

[project.scripts]
sprachassistent = "sprachassistent.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["sprachassistent*"]

[tool.setuptools.package-data]
sprachassistent = ["*.ico", "ui/*.html"]

[tool.pytest.ini_options]
testpaths = ["tests"]
